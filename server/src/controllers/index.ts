import { deleteTaskController } from './tasks';

import { addProject, getProjects } from './projects';
import { login, signup } from './user';

export {
  addProject, getProjects, login, signup, deleteTaskController,
};
