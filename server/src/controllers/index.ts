import { deleteTaskController } from './tasks';

import { login, signup } from './user';

import {
  addProject,
  getProjects,
  deleteProject,
  editProject,
  getSpecificProject,
} from './projects';

export {
  addProject,
  getProjects,
  deleteProject,
  login,
  signup,
  editProject,
  getSpecificProject,
  deleteTaskController,
};
